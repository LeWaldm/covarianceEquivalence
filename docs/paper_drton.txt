Fragen
- inwiefern ist die Annahme eines acyclic graph für die Struktur gerechtfertigt?
- X centered = mean 0 ?
- wir schätzen doch nur die topologische Ordnung, nicht die Stärke der einzelnen Verbindungen oder? Aber das zu erweiteren sollte doch kein Problem sein, wird doch referenziert?
- maximum in-degree bounded?



objective:
- get some information on this problem (computer algebra/computation/...)
- start with 3,4 variables
- possible equivalent classes
- idea: the general model (i.e. all variances different) has too much variability to estimate the direction of the covariance/dependence. In the specific model (i.e. all variances are equal) it is possible to estimate the complete covariance structure. So if we had a model, where certain blocks have the same variance, we could try to develop some theory to extract those blocks and determine the covariance structure within the blocks.
